export function respostaTipagemDinamica() {
    return {
        pergunta: "O que é tipagem dinâmica em JavaScript? Como é que ela difere da tipagem estática?",
        resposta: "Tipagem dinâmica significa que o tipo de uma variável é verificado em tempo de execução, e não em tempo de compilação. Em JavaScript, o tipo está associado ao valor, e não à variável, permitindo que a mesma variável armazene diferentes tipos de dados (como um número e depois uma string) ao longo do programa. A tipagem estática, por outro lado, exige que o tipo da variável seja declarado e fixo em tempo de compilação (como em Java ou C++), detectando erros de tipo antes da execução."
    };
}