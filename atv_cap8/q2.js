export function comparacaoModulos() {
    return {
        commonjs: "CommonJS (CJS) é estável e ainda amplamente usado em projetos Node.js",
        esmodules: "ES Modules (ESM) é a tendência e o padrão oficial do JavaScript",
        recomendacao: "ESM é recomendado para novos projetos JavaScript"
    };
}